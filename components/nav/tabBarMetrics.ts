// Shared tab-bar sizing so every scroll view leaves room for the bottom bar
// and nothing is hidden behind it. Screens add insets.bottom on top of this.
export const TAB_BAR_HEIGHT = 64; // visual height of the bar itself
export const TAB_BAR_SPACE = TAB_BAR_HEIGHT + 16; // height + breathing room for content
